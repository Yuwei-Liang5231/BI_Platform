"""异动检测（B10）：看板不等人来找问题。

三道判断的前置（B10-1 反常性）：
- 基准 = 过去 N 个**同期同类型**（同星期几）的日值分布——先排除周期性
  （如"周日天然低"的环比误报），再判断反常；
- z-score：|current - mean| / std >= z_threshold 判反常（指标级可配，
  默认 3.0 保守档）；样本数 < min_samples 不判断（宁漏不误）；
- 数值纪律：current 与基准全部来自 compute_metric_series 单点出口
  （缓存复用、与看板/问数口径同源），本模块零自产数字。

B10-2 在此之上叠要紧度与可归因；B10-3 消费批量接口做总览页与站内提醒。
"""
