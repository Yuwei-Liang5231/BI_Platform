# AI 原生 BI 平台 开发执行方案

> 交付主体：WorkBuddy Agent（vv）｜方案版本：V1.1｜编制日期：2026-09-10
> 依据：《AI原生BI平台项目开发计划》V1.4、《AI原生BI平台架构设计文档》V1.3
> 状态：**开工前决策已全部锁定，待你确认后启动 B0 + S1**

---

## 一、先说结论

1. **文档层面已完成闭环**，可以进入开发。计划 V1.4 / 架构 V1.3 / 评估报告 的周次、里程碑、人周、模块命名已交叉校验一致。
2. **前端规范与品牌标准已补齐**：PwC Core Library + 《前端开发规范V1.0.0》，判定为 **Product UI regime**，复用 Element Plus 品牌预设与 5 个品牌字体（资源均已核验存在），已写入架构 D2 / D18–D20 / §7.12。
3. **开工前 6 项决策已全部锁定**（业务数据 / 前端规范 / Python 版本 / 首批口径 / 运行方式 / 涨跌配色），阻塞项清零，详见第二节。
4. **平台必须行业无关**——这是你提的关键要求，我把它升级为架构硬约束（不变式 7 + D21）：合成电商数据只是验证载体，任何行业语义都不得进入编译器、取数服务与元数据模型；我会用**两套结构差异显著的数据集**交叉验证这一点。
5. **我按"依赖驱动"的批次推进，不是按文档章节顺序**。数据与编译器优先于页面——页面是最后一层皮。
6. **建议先做 MVP（批次 S1 + B0–B7，对应计划阶段 0+1）**，跑通端到端后再决定是否继续阶段 2/3/4a。
7. **全流程无法在一个会话内完成**。我会用进度文件保持连续性，每批次交付的都是可运行、已自测的代码。

---

## 二、已确认决策（2026-09-10 全部锁定）

| # | 事项 | 决策 | 落地位置 |
|---|---|---|---|
| 1 | **业务数据** | 先用**合成数据**跑通全链路，真实数据稍后替换。**关键：平台须支持各种业务数据，不能只适配电商** | 架构 **D21** + 批次 **S1**：生成 ≥2 套行业结构差异显著的数据集 |
| 2 | **前端规范与品牌** | PwC Core Library + 《前端开发规范V1.0.0》；Product UI regime | 架构 D2 / D18 / D20 + §7.12 |
| 3 | **Python 版本** | **3.13.12**（managed 独立 venv），源码保持 3.11+ 兼容 | 架构 **D22** |
| 4 | **首批口径** | 行业指标模板库（电商零售 / SaaS / 连锁餐饮 / 通用经营）冷启动 | 架构 D7 + 批次 B5 |
| 5 | **运行方式** | 本地 Windows 开发机，`uvicorn` + `npm run dev`，启动脚本化 | 见第四节 |
| 6 | **涨跌配色** | **红涨绿跌**（2026-09-15 用户修订：涨=红 `#D62222` PwC Error 500 / 跌=绿 `#059669` PwC Success 500，令牌 `--pwc-up`/`--pwc-down`），图标统一**三角**（▲/▼，不再叠加箭头）+ 文字 | 架构 **D19**（2026-09-15 修订） |

### 关于第 1 项的重要补充：行业无关性是硬约束

你的要求"支持各种业务数据，而非仅电商"是一个**架构级约束**，不只是数据问题。我的落实方式：

1. **写入不变式 7**：行业语义只允许通过两层表达——数据层（`datasets` / `dataset_relations` / `dataset_coverage`）与配置层（`calc_rule`、指标模板、`topic` 枚举）。编译器、取数服务、元数据模型中**禁止任何行业分支、行业专属字段或硬编码业务规则**。
2. **合成数据不能只用电商**。只用一套电商数据验证，等于只证明了"电商能跑通"，无法暴露行业适配缺陷。我生成**两套结构差异显著**的数据集：

| 数据集 | 行业形态 | 表结构 | 刻意制造的差异 |
|---|---|---|---|
| **A 零售交易型** | 电商零售 | orders / users / products / channels | 订单粒度、退款与满减、品类×区域维度 |
| **B 订阅服务型** | SaaS | subscriptions / accounts / plans / usage_events | 订阅周期、经常性收入、账户粒度、无"订单"概念 |

3. **验收看差异而非看数量**：同一套编译器在 A/B 上独立完成"定义 → 编译 → 取数 → 导出"，**不修改任何产品代码**。若 B 的订阅周期类语义超出 v1 算子，必须被**明确拒绝并提示**，而不是静默算出错误结果——这恰好能在阶段 1 早期验证算子清单的边界。
4. 两套数据都包含 **GBK 编码样本与混杂类型列**，顺带验证接入质检。

---

## 三、我的开发原则

| 原则 | 具体做法 |
|---|---|
| **可运行优先于完整** | 每批次结束必须能启动、能请求、能看到结果；不做"写了一半的模块" |
| **测试先行于编译器** | 指标编译器是全项目核心难点，先写 golden SQL 测试用例（定义 → 期望 SQL），再写实现 |
| **契约先于实现** | 先冻结 `ApiResponse` 与接口清单（含 HTTP 码与 body code 对齐），前后端据此并行 |
| **依赖驱动排序** | 数据接入 → 指标元数据 → 编译器 → 取数服务 → 权限 → 前端。页面最后做 |
| **不交付未验证的代码** | 每批次我都实际用 Bash 跑通 `pytest` 与启动冒烟，再交付给你 |
| **每批有可验看清单** | 交付时明确告诉你"打开哪个页面、点什么、应该看到什么"，你能自己确认 |
| **不替业务做决策** | 口径定义、仲裁、验收结论由业务方出；我只提供工具与默认值 |
| **行业无关是硬约束** | 合成数据只是验证载体。任何行业语义不得进入编译器、取数服务、元数据模型；每涉及行业相关逻辑，我都用 A/B 两套数据集交叉验证 |

---

## 四、技术环境准备

| 项 | 方案 |
|---|---|
| 后端运行时 | **Python 3.13.12**（managed 独立 venv，D22）；源码保持 3.11+ 兼容；依赖锁定 `requirements.txt` |
| 后端框架 | FastAPI + uvicorn（单进程，D8）+ SQLAlchemy 2.x + DuckDB + pyarrow |
| 前端运行时 | Node 22.22.2（managed，满足规范 `^20.19.0 \|\| >=22.12.0`），npm + `package-lock.json` |
| 前端版本 | Vue 3.5.34 / Vite 8.0.13 / Pinia 3.0.4 / Vue Router 5.0.7 / Element Plus 2.14.0 / Axios 1.16.1 / Sass 1.99 / ECharts 6.1.0（精确补丁版本，禁 `^` 漂移） |
| 品牌样式基座 | 复用 `pwc-ui-skill/presets/element-plus/src/styles/`（tokens + element-overrides + index）→ `src/styles/` |
| 品牌字体 | 5 个 TTF 从 `~/.workbuddy/skills/pwc-brand/fonts/` → `public/fonts/`，标准 `@font-face` + CJK 回退 |
| LLM | 阶段 2 起启用，复用工作区 `env`（DeepSeek 兼容接口），`base_url/api_key/model` 全配置化 |
| 数据与元数据 | `data/uploads/` + `data/parquet/` + `data/metadata.db`，全部本地文件，gitignore |
| 测试 | pytest + FastAPI TestClient；前端 `npm run build:{test,uat,pro}` 四环境校验 |
| 隔离原则 | 所有包装进 managed venv / workspace，**不污染全局环境** |

---

## 五、开发批次划分（依赖驱动）

### MVP 段：对应计划阶段 0+1（目标：指标口径同源 + 看板可验收）

| 批次 | 内容 | 关键产出 | 我能自测的验收 |
|---|---|---|---|
| **B0** | 工程脚手架与地基 | 目录骨架、`core/config`（四环境 `.env`）、`core/response`（ApiResponse + 码值对齐）、`infra/repository` 抽象 + SQLite 实现、`infra/storage`、日志、health 接口 | `pytest` 通过；`uvicorn` 启动 `/api/health` 返回 200 |
| **S1** | 多行业合成数据（D21） | `scripts/gen_synthetic_data.py`：数据集 **A 零售交易型**（orders/users/products/channels）+ 数据集 **B 订阅服务型**（subscriptions/accounts/plans/usage_events，含订阅周期）。两套均含 GBK 编码样本与混杂类型列，10–100 万行可配 | 两套数据均可被 B1 正确接入；字段/表关系/覆盖区间识别正确；**代码中无行业硬编码**（grep 校验） |
| **B1** | 数据接入（阶段 1 最小版，D12） | 上传 → 编码识别（UTF-8/GBK/GB18030）→ 类型推断（混杂列标记）→ pyarrow 落 Parquet → `datasets` 注册 → `dataset_relations` 可视化配置 → `dataset_coverage` 计算；API + `/datasets` 页 | 上传一份 GBK + 混杂类型 CSV，能识别、能定位异常行、覆盖区间正确 |
| **B2** | 指标元数据 + 编译器（**核心难点**） | `metrics` CRUD（含 `level`/`parent_id`/`disambiguation`）、`calc_rule` JSON Schema 校验、`compile()` 生成参数化 SQL（含 Join 与周期元信息）、`metric_sql_archive` 原子同步、别名搜索、版本与 `metric_changes` 留痕 | **golden SQL 测试 ≥ 20 用例**；超范围算子/缺表关系保存即拒绝并明确报错 |
| **B3** | 统一计算服务 query | `POST /api/query/metric-value`、权限过滤、周期完整性（不完整返回 `null`）、LRU 缓存（键含 `ver` + `dataset_ver`）、`export` 挂载于 query | **口径一致性固定用例**：看板 / 取数 / 导出三处数值完全一致；改口径后旧缓存失效 |
| **B4** | auth 与权限双出口 | JWT 登录、用户/角色（最小本地账号 D10）、可见性判定、`query` 数值出口 + 元数据接口双过滤、401/403/400 与 body code 对齐 | API 测试覆盖全部阶段 1 接口的 401/403/400 场景 |
| **B5** | 行业指标模板库 | 4 个行业 YAML（约 100 条，含别名/层级/口径分歧说明/默认算法建议）、导入向导与幂等 | 模板导入幂等；依赖扩展算子的指标标记为"暂不可计算" |
| **B6** | 前端主体（含品牌落地） | 6 个页面：登录、指标目录（层级展开 + 别名搜索 + 当前值/环比/小趋势）、指标详情（折叠 SQL）、统一看板（ECharts）、指标管理、数据集与表关系；**PwC 样式基座 + 品牌字体 + Logo 内联 SVG + 组件四态 + 涨跌语义（D19）** | 四环境 `build:{dev,test,uat,pro}` 全通过；无裸 axios；不完整周期显示"—"；通过 §7.12 品牌自检 10 项 |
| **B7** | MVP 联调与验收 | 端到端跑通：接入 → 定义 → 编译 → 看板 → 改口径全局生效；口径变更影响清单 | 我出一份**自测报告 + 你验看清单**，含走查步骤 |

### 后续段（MVP 验收后再启动）

| 批次 | 对应阶段 | 内容 |
|---|---|---|
| B8 | 阶段 2 | 完整质检（行列级定位）、增量导入、`dataset_coverage` 刷新与缓存失效 |
| B9 | 阶段 2 | 问数 Agent（Deep Agents）、意图解析、理解卡、歧义黄提示（引用 `disambiguation`）、"算不了"返回、问数测试集回归 |
| B10 | 阶段 3 | 异动检测引擎、周期修正、三道过滤基线算法、人工标注基线集 |
| B11 | 阶段 3 | 触达适配器（企微 → SMTP → 站内）、限流、送达重试与 `notify_log`、经营总览页 |
| B12 | 阶段 3 | 归因分解、报告生成（python-docx）、数字溯源、口径变更影响分析与历史口径回看 |
| **M1a** | 阶段 1 末 | **自动建模技术预研（spike）**，产出架构 7.11 结论，决定 B13 范围 |
| B13 | 阶段 4a | 自动建模引擎（或降级方案）、指标上线效率优化（≤1 天） |
| B14 | 阶段 4a | 多层归因（品类 → 区域 → 渠道 → 商品） |
| B15 | 阶段 4b（可延后） | 行级权限、SSO/LDAP、审批流与审计、目录扩至 60+、运营 SOP 与培训材料 |

---

## 六、质量保障方式

| 层 | 我的做法 |
|---|---|
| 单元测试 | 编译器 golden SQL、权限判定（默认可见/受限登记/目录过滤）、周期完整性（不完整返回 `null` 而非 0）、模板导入幂等 |
| 集成测试 | **口径一致性固定用例**（看板/取数/导出三处一致；改口径后缓存失效；受限指标四端行为一致）、数据接入后 `dataset_ver` 递增与缓存失效 |
| **跨行业一致性** | 同一套编译器在 A（零售交易型）与 B（订阅服务型）两套数据集上独立完成"定义 → 编译 → 取数 → 导出"，golden SQL 用例两行业各 ≥8 条；断言无需改产品代码即支持新行业；超纲语义须明确拒绝而非静默出错 |
| API 测试 | FastAPI TestClient 覆盖全部接口，含 401/403/400 与码值对齐断言 |
| 前端 | 四环境 `build` 全通过 + 规范自检清单 |
| 阶段 3 追加 | 异动算法回归（基于人工标注集）、触达送达失败与重试测试 |
| 交付前 | 我实际用 Bash 跑通测试与启动冒烟，把输出附在交付说明里 |

---

## 七、我做不了、需要你的部分（边界声明）

| 事项 | 说明 |
|---|---|
| 阶段 0 业务调研与口径梳理 | 需业务方访谈、口径冲突仲裁、管理层确认仲裁人层级。我只能提供模板库冷启动与访谈提纲 |
| 业务验收与培训 | 验收签字、两轮培训与考核由你组织 |
| 真实业务数据 | 需你提供（脱敏） |
| 阶段 3 触达凭据 | 企业微信 webhook、SMTP 账号需你申请 |
| 阶段 4a 自动建模的可行性判断 | M1a 预研我可做技术验证，但最终是否采纳需数据团队评审 |
| 7×24 连续开发 | 我会分批次推进，用进度文件保证连续性 |

---

## 八、连续性保障

全流程代码量大，我会维护一个 `PROGRESS.md`（工作区根目录），每次交付后更新：

```
批次 | 状态 | 完成日期 | 关键文件 | 自测结果 | 阻塞项 | 下一步
```

内容包括：已完成的模块清单、已冻结的接口契约、尚未解决的技术债务、待你决策的事项。任何一次新会话我都会先读它再继续，不依赖对话记忆。

---

## 九、建议的启动方式

**第一步（等你确认）**：执行 **B0 + S1**——工程骨架 + 两套多行业合成数据（零售交易型 + 订阅服务型）。

**第二步**：执行 B1，交付一个"能上传数据、能看到数据集与表关系"的可运行骨架。此时你第一次能真实触摸系统，并可直接用两套数据验证行业无关性。

**第三步**：B2 + B3（编译器与取数服务）——这是项目的价值核心，也是我花最多测试的地方。

**第四步**：B4–B7，前端成型，MVP 端到端验收。

**第五步**：拿着 MVP 的真实效果，再决定要不要继续 B8–B15。

---

*本方案为执行层约定，与计划 V1.4 / 架构 V1.1 保持一致；批次划分随实际进展在 `PROGRESS.md` 中更新。*

---

## 十、阶段 2 详细设计（V1.2 增补，2026-09-14）

> MVP（B0–B7 + 全量回归）已于 2026-09-11/14 验收通过。本节回答架构文档第九节的 4 个"阶段 2 详细设计必答项"，并细化 B8/B9 批次范围。

### 10.1 必答项 2：增量导入策略

**决策：分片 Parquet 目录组织 + append/replace 双模式。**

| 项 | 决策 |
|---|---|
| 存储组织 | 新数据集落盘为 `data/parquet/{name}/part-0001.parquet`，`datasets.parquet_path` 记录**目录**；追加写新分片 `part-{N:04d}.parquet`，不重写历史数据（O(增量) 而非 O(全量)） |
| 存量兼容 | B1 时代的单文件数据集（`parquet_path` 指向 .parquet 文件）原样可读；首次 append/replace 时自动迁移为目录（旧文件移入为 part-0001），迁移失败（文件占用）→ 400 明确报错 |
| append | 表头列名集合必须与现有 schema **完全一致**（顺序可不同，写入时按现有 schema 重排）；新行按现有列类型转换（混杂列已是 string 接受任意值；转换失败 400 带行号+列名+原始值）；不做去重（重复责任在数据方，质检报告可见） |
| replace | 清空分片目录重写 part-0001，语义 = 全量覆盖；schema 校验与 append 相同 |
| dataset_ver | append/replace 成功后在**同一事务**内 +1 → 指标缓存键含 dataset_ver → 自然失效（机制 B3 已备，本次补端到端测试） |
| 审计 | 原始上传件保留于 `data/uploads/{name}__{mode}__{ts}{ext}` |
| API | `POST /api/datasets/{id}/data`（multipart: file + mode=append\|replace，默认 append；admin 专用）；响应 = 数据集详情 + `{mode, rows_added, dataset_ver, coverage}` |

### 10.2 必答项 3：覆盖区间刷新触发

**决策：导入事务内自动刷新，无定时调度。** 本地单机部署，导入即生效：重算所有分片 date/datetime 列的 min/max/non-null 并整体替换 `dataset_coverage` 行，与 `dataset_ver+1` 同事务提交。缓存失效依赖 dataset_ver（指标定义未变而数据变化同样生效——正是契约 v2 缓存键含覆盖端点的补充保险）。

### 10.3 必答项 4：质检结果呈现（行列级定位）

**决策：按需计算的质检报告 API + 前端质检面板；"可操作修正"通过 replace 重导闭环。**

- `GET /api/datasets/{id}/quality`（登录即可）：逐列报告 `{name, type, mixed, null_count, null_ratio, distinct_count, is_empty(全空列), is_constant(常量列), issues:[{level: high/medium/info, code, message}], deviations(mixed 列: 行号+原始值+实际类型, 前 20), null_sample_rows(非字符串列前 10 个空值行号)}` + 整体 `coverage`。
- issue 等级：high = 空列/混杂列（影响计算口径）；medium = null 率 > 50%；info = 常量列（区分度低）。
- 前端：数据集页新增「质检报告」抽屉（列清单 + 问题徽标 + 异常行展开）；上传/增量导入成功后引导查看。
- 修正路径：混杂/异常列 → 数据方修正文件 → `POST /{id}/data` replace 重导（ver+1 全局刷新）——不做在线单元格编辑（超出 BI 平台职责，数据主权留在源系统）。

### 10.4 B9 问数 Agent 设计要点

| 项 | 决策 |
|---|---|
| LLM 边界（计划铁律） | LLM **只产意图**（选指标/时间/维度/排序），数值一律由指标中心统一计算；LLM 不写 SQL、不产数字 |
| 配置 | `llm_base_url/llm_api_key/llm_model`（config 已预留）；未配置时问数接口 400 明确提示，页面入口置灰 |
| 解析器 | `infra/llm` 适配器（OpenAI 兼容 chat.completions，JSON mode）+ 确定性关键词兜底解析器（无 LLM/LLM 失败时降级，保证测试集可离线回归） |
| API | `POST /api/query/ask`（body {question}）→ 理解卡 `{metric_code, start, end, compare, ambiguous:[{field, options, default, reason}], can_compute, no_metric_reason?}`；`POST /api/query/ask/execute`（确认后的卡）→ 与看板同一 compute_metric_value（权限同源） |
| 歧义 | 理解卡黄色提示引用 `metrics.disambiguation` 登记的默认算法（如"下降"默认按减少金额）；指标找不到/算不了（超纲语义）明确返回，不现场拼装查询 |
| 权限继承 | ask/execute 走与看板同一 `restricted_metric_ids` 判定；无权限指标理解卡直接提示"该指标无权限" |
| 回归 | 问数测试集 `backend/tests/ask_testset.json`（问题 → 期望指标/时间/歧义标记），pytest 断言（兜底解析器离线跑）+ 走查脚本（LLM 在线跑，可跳过） |

### 10.5 批次划分（阶段 2）

| 批次 | 内容 | 验收 |
|---|---|---|
| B8-1 | 质检报告 API + 前端质检面板 | 构造 CSV（混杂/空列/常量/高 null）行列级定位准确 |
| B8-2 | 增量导入（append/replace）+ 目录组织迁移 + ver 递增 + 覆盖刷新 + 缓存失效端到端 | append 后同区间取数 cache=miss 且值更新；schema 不符 400 |
| B8-3 | 测试 + 走查 + 文档 + 提交 | pytest 全过 + smoke 全过 |
| B9 | 问数 Agent（解析器 + 理解卡 + execute + 权限 + 测试集） | M3/M4 里程碑标准 |

---

## 十一、剩余阶段批次细化（V1.3 增补 2026-09-15；V1.3.1 修订：B11 转 backlog、多轮追问纳入本期；V1.3.2 修订 2026-09-15：问数易用性三件套并入 B9.2-3、新增 B9.2-5 回答观感升级、三项转 backlog；V1.3.3 修订 2026-09-15：新增 B9.2-6 会话改版【布局+持久化】并已交付）

> 依据：① 对标《别再给看板加聊天框了》（BI佐罗，2026-09-03）的 AI 原生 BI 核心理念——**AI 从数据接入就参与、口径只定义一次、问数先摊开理解、看板主动找问题、报告算写分离**；② 结合当前已完成功能的缺口盘点。本节将原"后续段"（B10–B15）与新增升级项全部细化为 MVP 同款颗粒度批次，**逐批推进，每批交付可运行、已自测的代码**。

### 11.1 现状盘点（截至 2026-09-15）

| 已完成 | 说明 |
|---|---|
| MVP 全链路（B0–B7） | 接入→编译器→取数→权限→模板→前端 6 页→验收 |
| B8 质检 + 增量导入 | 行列级质检报告；append/replace 分片 Parquet；ver 失效链路 |
| B9 问数 v1 | 单指标 + 时间 + 环比；理解卡 + 歧义提示 + 权限继承；测试集回归 |
| LLM 模型管理（60a4bd0） | `llm_models` 表 + 管理页（增删/切换/测试连通）；配置优先级 DB > env；api_key 脱敏 |
| 契约资产 | 周期完整性 v2 / 全期常数 v3 / 半开区间 / 缓存键含 dataset_ver；pytest 224 |

| 升级缺口（对标文章） | 影响 |
|---|---|
| 问数只支持"查一个数"，不支持"华东区哪些品类掉得最厉害"式问题 | AI 作用未发挥：缺**维度拆解、筛选、排序、TopN** |
| 看板完全被动，没人盯就不发现问题 | 缺**异动检测**（反常性/要紧度/可归因三道判断 + 周期性基准） |
| 报告中心未启动，且原设计只是"报告生成" | 应升级为**算写分离**：平台先算结论（含归因分解），LLM 只组织文字、数字用占位符回填，每个数字可对账 |
| 质检报告只有结构化数据 | AI 可补一段修复建议叙述（小项） |

### 11.2 批次总览与推进顺序

```
B9.2 问数 2.0（5 子批，含多轮追问） → B10 异动检测（3 子批，含站内提醒最小版） → B12 报告中心（4 子批）
→ B13 自动建模（预研+实施） → B14 多层归因 → B15 企业化（可延后）
```

> 顺序说明：报告中心（B12）需要异动+归因结论做素材（B10 产出），故先 B10 后 B12；多层归因保留为 B14。原计划 B12（归因+报告）的内容拆入 B10-2（单层归因）与本节 B12（报告）。
>
> **V1.3.1 修订（2026-09-15，用户确认）**：① 触达适配（B11：企微/SMTP）**移出主线、转 backlog**——现阶段平台无真实用户群，"管理层不看平台"前提不成立，且企微/SMTP 凭据申请是额外负担；文章原型的"主动"完全由经营总览页承载，故仅在 **B10-3 并入零成本的站内提醒最小版**（小红点/站内通知，平台内闭环）作为"发现→告知"的替代。backlog 触发条件：接入真实数据、出现"有人需要但不会主动打开平台"的真实反馈时再启动。② 多轮追问（"那上个月呢"）**纳入本期**，作为 B9.2-4。

### 11.3 批次明细

#### B9.2 AI 问数 2.0：从"查一个数"到"回答一个业务问题"

| 子批 | 内容 | 关键产出 | 自测验收 |
|---|---|---|---|
| **B9.2-1** 维度拆解计算出口 | `compute_metric_breakdown`：GROUP BY 维度列 + 维度筛选 + 排序（按绝对变化/变化率/当期值）+ TopN；维度列候选 = 主数据集与可 JOIN 维表的**低基数文本列**（来自数据层配置，行业无关）；与编译器/缓存/权限同源；比率类指标拆解按分子分母重算（禁止对"率"求平均） | query 路由扩展 + golden 测试 | pytest 新增 ≥6 用例：**加性指标拆解合计 ≈ 单值**；比率指标口径正确；维度列越纲 400 |
| **B9.2-2** 意图扩展 + 理解卡 + 前端 | intent schema 增 `dimensions/filters/order_by/top_n`；LLM 候选清单附带维度列（**防幻觉铁律不变**：只能选清单内，越纲丢弃）；关键词解析器扩展（"按X拆/分维度""前N名""哪个最…""地区=华东"）；理解卡展示新字段且全部可改；问数页自适应：单值卡（无维度）/ 拆解表格（有维度，可点入指标详情）；沿用 seq 守卫 | 问数页升级 + 测试集扩充 | `ask_testset.json` 扩至 ≥14 例（维度类 ≥8）**离线回归 100%**；smoke_ask 扩展；受限指标行为与 v1 一致 |
| **B9.2-3** 回归 + 问数易用性三件套 + 提交 | **V1.3.2 扩充（用户确认）**：① 指标别名补全——后端 `aliases_json`/`_metric_candidates`/LLM 候选清单已支持，补指标管理页别名编辑确认与测试用例；② 空态推荐问题——`GET /query/ask/suggestions` 从登录用户可见指标自动生成 3~5 个示例问题（单值/拆解模板），前端空态点击即问；③ 多指标并列问法——问句命中多个指标时不再只取最具体一个，返回多张理解卡（数值仍全部走 compute 单点出口）；顺手修「删除指标/数据集后看板残留选中态」（PROGRESS 已登记技术债）；全量回归；文档更新 | ask 路由扩展 + 前端空态/多卡 + 提交 | pytest 全过（新增别名/建议/多指标用例）+ smoke 全过 + 四环境 build |
| **B9.2-4** 多轮追问（会话上下文） | `/query/ask` 增 `session_id`：后端按会话暂存上一轮意图（内存 + TTL，不落库）；追问判定 = 问题中**无指标且无维度信息**（仅时间词/"它/这个/呢"等指代）→ 继承上一轮 metric/dimensions/filters/order_by，时间按新词重算；"那上个月呢""只看华东的"均走此链路；前端问数页改会话式消息列表（理解卡内嵌气泡）；权限每轮重校验（继承指标若已受限 → 提示无权限，不泄露名称）；会话过期/首问降级为单轮 | ask 接口扩展 + 会话上下文管理器 + 前端会话改造 | `ask_testset.json` 增多轮用例 ≥4 例（继承时间/继承维度/重置话题/会话过期）**离线回归 100%**；smoke_ask 增追问步骤 |
| **B9.2-5** 回答观感升级（V1.3.2 新增，用户确认） | ④ 一句话结论模板——后端**模板拼装**结论句（数值全部来自 compute 单点出口填入模板，**严禁 LLM 生成数字**，算写分离红线不破），观感从"返回一个数"升级为"回答业务问题"；⑤ 拆解占比可视化——拆解表格加占比列 + 迷你条形（贡献度一目了然，B14 多层归因的轻量前菜）；⑥ 口径透明脚注——理解卡/结果卡可展开查看指标 calc_rule 口径摘要（尤其比率指标），让用户"敢引用" | 后端结论拼装 + 前端结论条/占比列/口径抽屉 | 构造用例结论句数字与接口返回一致（对账）；拆解占比合计=100%；pytest 全过 + 四环境 build |
| **B9.2-6** 会话改版：三栏布局 + 会话持久化（V1.3.3 新增 2026-09-15，用户确认方案 B；**已交付**） | ① 三栏布局——左侧历史会话列表（可折叠/最近使用排序/删除）+ 右侧消息流（历史轮只读、当前轮理解卡可交互、计算后自动折叠摘要）+ 底部固定输入条 + 空态欢迎屏；② 持久化——`ask_conversations`/`ask_messages` 表：每轮落库问句 + 理解卡快照 + 结果快照（恢复不重算），恢复续聊/删除/重命名 API，按用户严格隔离（非本人报"会话不存在"）；追问意图继承仍走内存 TTL（键 `conv-{id}`）；③ 附带——结果卡一键导出 CSV（口径同展示）、中文数字 TopN、基期口径文案明确化、推荐问题跨指标轮转 + 业务可读名优先、提交后清空输入框 | 会话表 + conversations API + 前端三栏改造 + AskResultCard 抽取 | pytest 256 passed（+会话生命周期/隔离/重命名/中文数字）；smoke 14/14；四环境 build |

> **V1.3.3 说明（2026-09-15）**：B9.2-6 为用户实测反馈驱动的插队批次，原 backlog「会话历史留存」与「结果导出」随之交付；backlog 仅余「没听懂反馈闭环」「结果一键加入看板」。执行顺序：~~B9.2-3 → B9.2-4~~（已完成）→ **B9.2-5 → B9.2-6（已完成）** → B10。

> 注：多轮追问原登记为 backlog（B16），**V1.3.1 起纳入本期**（用户确认）。上下文仅存意图（指标/维度/筛选），数值每轮仍由 compute_metric_value 单点出口现算，红线 1/2 不变。
>
> **V1.3.2 修订（2026-09-15，用户确认）**：① 问数易用性三件套（指标别名/空态推荐问题/多指标并列）并入 B9.2-3；② 新增 B9.2-5 回答观感升级（一句话结论模板/拆解占比可视化/口径透明脚注）；③ 没听懂反馈闭环、会话历史留存、结果一键加入看板三项**转 backlog**（价值明确但非主干，待 B9.2-4/B10 交付后视反馈启动）。执行顺序：B9.2-3 → B9.2-4 → B9.2-5 → B10。

#### B10 异动检测：看板不等人来找问题

| 子批 | 内容 | 关键产出 | 自测验收 |
|---|---|---|---|
| **B10-1** 反常性引擎 | 基准 = **过去 N 个同期同类型**的分布（同星期几 / 上月同段），先排除周期性再判断；z-score / 分位数阈值（指标级可配，默认值给保守档）；结果结构 `{metric, current, baseline, delta, direction, abnormality}` | 异动计算服务 + 阈值配置 | 构造周期性数据：**周日 vs 前 6 天的环比误报被排除**；真实突刺检出；标注集回归 |
| **B10-2** 要紧度 + 单层归因分解 | ②要紧度（变化幅度/影响阈值过滤）；③可归因（对变化量按单一维度拆贡献度取 Top 来源，**归因算法独立成服务**供报告复用）；三道判断全过才生成异动结论 | 归因 API（`POST /query/attribute`）+ 结论结构含 `top_dimensions:[{value, contribution}]` | 手算可对账的构造案例：贡献度数值正确且守恒（各维度贡献合计 = 总变化）；标注集 ≥20 例回归 |
| **B10-3** 经营总览页 + 看板联动 + 站内提醒最小版 | 总览页：异动卡片（当前值/正常水平/变化/主要来源），**限量 3~5 条宁缺毋滥**，空态明确"本期无异动"；看板顶部黄条提示异常指标；点击卡片进指标详情；**站内提醒最小版**：有未读异动时导航栏小红点 + 站内通知列表（`notifications` 表，B10 异动结论落库即生成，纯平台内零外部凭据，B11 backlog 启动前承载"发现→告知"闭环） | `/overview` 页 + 站内通知 + 走查脚本 | 构造场景走查：异动出现/消失/限量生效；小红点未读/已读状态正确；前端 build 全过 |

#### B12 报告中心：算写分离，数字绝不交给 AI 猜

| 子批 | 内容 | 关键产出 | 自测验收 |
|---|---|---|---|
| **B12-1** 结论计算 + 模板渲染（**无 LLM 完整可用版**） | 报告模板配置（日报/周报/月报 × 指标集 × 章节开关）；**平台先算结论集**：每指标值/环比/同比 + 引用 B10 异动 + B10-2 归因 TopN；规则化叙述句（预置句式库）拼装；报告页预览 + 打印/复制 | `report_templates` 配置 + 结论计算服务 + `/reports` 页 | **LLM 关闭状态下生成完整报告**；报告内每个数字与 metric-value 接口对账一致 |
| **B12-2** LLM 叙述层（算写分离核心） | 结论 JSON → LLM 生成章节叙述，**数字一律用占位符 `{{ref:结论ID}}`**，渲染时平台回填真值；占位符校验（引用不存在/漏回填 → 该段降级为模板句）；叙述层禁裸数字（正则审计，LLM 自产数字一律剔除）；可选小项：质检报告修复建议叙述 | `infra/llm` 之上的叙述生成器 + 报告渲染管线 | mock LLM 测试：占位符 100% 正确回填；构造 LLM 输出幻觉数字 → 被剔除并降级；降级链路可用 |
| **B12-3** 报告存档与历史 | 报告按"模板×周期"实例化存档（含版本），历史列表 + 查看 + 重新生成（幂等/版本递增） | `report_instances` 表 + 历史页 | 同周期重生成版本管理正确；历史数字不受后续口径变更影响（快照语义） |
| **B12-4** 回归 + 提交 | 全量回归 + 文档 + 提交 | 提交 | pytest + smoke + build 全过 |

#### B11 触达适配（**已转 backlog，V1.3.1 起不在主线**）

> 转入 backlog 理由（用户确认，2026-09-15）：现阶段平台为本地单机 + 合成数据、无真实用户群，"管理层不打开平台"前提不成立；企微/SMTP 需申请外部凭据，适配器/重试/限流是纯工程量；文章原型的"主动"完全由经营总览页承载，站内提醒最小版（B10-3）已在平台内闭环"发现→告知"。以下内容保留，供 backlog 启动时直接引用。

| 子批 | 内容 | 关键产出 | 自测验收 |
|---|---|---|---|
| **B11-1** 通知适配器 | 企微 webhook / SMTP / 站内信三适配器；`notify_log` + 限流 + 送达重试；凭据只放 `.env.local`（复用 LLM 配置的本地覆盖机制） | `infra/notify` + 通知中心页（站内信） | mock SMTP/webhook：送达成功/失败重试/限流测试全过；站内信端到端 |
| **B11-2** 推送编排 | 「立即推送」日报（报告中心实例化 + 异动摘要）；定时触发本地先可选 Windows 任务计划，部署形态确定后演进 | 推送按钮 + 编排服务 | 端到端推送一条日报并留痕 |

> **启动条件**：接入真实数据 + 出现"有人需要但不会主动打开平台"的真实使用反馈。

#### B13 自动建模（先预研后实施，对应 M1a）

| 子批 | 内容 | 关键产出 | 自测验收 |
|---|---|---|---|
| **B13-1** 预研 spike | 对 A/B 两套合成数据自动**建议**表关系（值域重叠/命名相似/基数统计 + LLM 辅助语义判断）与指标候选（模板匹配 + 列名语义）；只出建议不自动入库 | 预研报告（准确率 + 可行性结论，回答架构 7.11） | 建议结果人工核对清单；明确"哪些场景必须人工" |
| **B13-2** 实施（视预研结论） | 关系/指标**建议确认流**（人确认才入库）；指标上线效率目标 ≤1 天 | 建模建议向导 | 从上传到指标可看 ≤1 天流程走查 |

#### B14 多层归因

| 内容 | 关键产出 | 自测验收 |
|---|---|---|
| 维度层级树（品类→区域→渠道→商品，层级来自指标 dimensions 配置）逐层下钻贡献度；报告/总览复用同一归因服务 | 归因树 API + 详情页下钻视图 | 构造数据：树形贡献**逐层守恒**；叶子合计 = 总变化 |

#### B15 企业化（可延后，按需启动）

行级权限（B15 原范围）、SSO/LDAP、口径审批流与审计增强、指标目录扩至 60+、运营 SOP 与培训材料。**拆分待启动前再做细化。**

### 11.4 贯穿各批的设计红线（验收时逐条检查）

1. **数值单点出口不变**：breakdown / 归因 / 报告结论全部走同一编译器与 query 服务，禁止任何旁路 SQL。
2. **LLM 只产意图与文字，永不产数字**：报告数字必须占位符回填；意图必须候选清单内选择，越纲丢弃。
3. **无 LLM 必须可用**：所有 LLM 环节有确定性降级（关键词解析 / 模板句 / 规则化报告）——公司网络屏蔽外网不影响功能。
4. **行业无关**：维度列、归因层级、报告指标集全部来自数据层与配置层，代码零行业语义。
5. **权限同源**：问数 / 总览 / 报告统一走 `restricted_metric_ids` 判定，受限指标不泄露名称。
6. **缓存纪律**：新计算出口纳入现有 ver/dataset_ver 失效机制。
7. **每批收尾**：pytest 全过 + smoke + 四环境 build + commit（push 视网络）+ PROGRESS 更新。

### 11.5 需要你配合的事项

| 事项 | 时点 |
|---|---|
| ~~确认本节批次划分与推进顺序~~ | **已确认**（2026-09-15：B11 转 backlog、多轮追问纳入本期） |
| ~~多轮追问是否纳入~~ | **已定：纳入本期（B9.2-4）** |
| 企微机器人 webhook / SMTP 账号 | backlog B11 启动前（触发条件见 11.2 修订说明） |
| 真实业务数据（脱敏）替换合成数据 | 任意批次后均可 |
| 各批次验收签字 | 每批交付时 |
