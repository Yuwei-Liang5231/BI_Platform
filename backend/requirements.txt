# B0 依赖（架构 D8 / D22：FastAPI 单体 + SQLAlchemy 2 + SQLite 元数据）
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
# B4 auth（JWT 登录）
pyjwt>=2.8
# B2 起计算引擎（Parquet 直查，架构 7.2/7.3）
duckdb>=1.0
# 数据接入
pyarrow>=15
openpyxl>=3.1
charset-normalizer>=3.3
# B14.1 xlsx 解析加速（可选：Rust 引擎，比 openpyxl 快 5~10 倍，日期原生解析；
# 未安装时自动回退 openpyxl，功能等价仅速度差异）
python-calamine>=0.8
# B5 行业指标模板库（YAML 模板包）
pyyaml>=6.0
# 测试
pytest>=8.0
httpx>=0.27
