# 开发环境（默认）
APP_ENV=dev
LOG_LEVEL=DEBUG
DATA_DIR=../data
API_PREFIX=/api
# 阶段 2 起启用；LLM（DeepSeek 等 OpenAI 兼容接口）。
# 真实凭据不要写在本文件（本文件进 git）——写入同目录 .env.dev.local（已被 .gitignore 忽略），
# 或运行时在「模型管理」页登记启用（DB 记录优先于 env）。
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
